import {Router} from "express"

const sharingRouter: Router=Router()

sharingRouter.post("/share", (req,res)=>{

})

sharingRouter.post("/:shareLink", (req,res)=>{
    
})

export default sharingRouter